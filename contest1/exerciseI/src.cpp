#include <bits/stdc++.h>

using namespace std;
int main(){
    int N, K;
    cin >> N >> K;
    vector<pair<double, double>> points;
    
    double x, y;
    for(int i{0}; i < N; ++i){
        cin >> x >> y;
        points.push_back({x, y});
    }

    

    return 0;
}