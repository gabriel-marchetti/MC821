#include <bits/stdc++.h>

int main(){
    int num_cases;
    long long a, b;
    std::cin >> num_cases;

    for(int i{0}; i < num_cases; i++){
        std::cin >> a >> b;
        std::cout << a + b << '\n'; 
    }


    return 0;
}