#include <bits/stdc++.h>
using namespace std;

int main(){
  double a, b, c, d, acc_prob{0}, aux_prob{1};
  cin >> a >> b >> c >> d;
  double prob1 = a / b, prob2 = c / d;
  while(aux_prob > 1e-6){
    acc_prob += prob1 * prob_aux;
    prob_a
  } 

  return 0;
}
